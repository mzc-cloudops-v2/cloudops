enabled: true

image:
  repository: redis
  tag: "7.4-alpine"
  pullPolicy: IfNotPresent

port: 6379

service:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
