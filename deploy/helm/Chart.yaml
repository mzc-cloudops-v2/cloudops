apiVersion: v2
name: cloudops
description: CloudOps Umbrella Helm Chart
type: application
version: 0.0.1
appVersion: "0.0.1"

# core / nats / redis / seaweedfs 는 charts/ 로컬 subchart 로 자동 포함.
# plugin 은 templates/plugins/ 가 values.plugins 를 렌더 (개별 차트 없음).
